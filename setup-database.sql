-- GCE Smart Notify Database Setup Script
-- Run this in PostgreSQL to create the database

-- Create database (if it doesn't exist)
-- Note: You may need to run this as a PostgreSQL superuser
CREATE DATABASE gce_smart_notify;

-- The application will automatically create all tables on first run
-- No need to run any other SQL commands

-- To verify the database was created:
-- \l (list all databases)
-- \c gce_smart_notify (connect to the database)
